# Supabase credentials (Netlify Supabase integration provisions these automatically)
SUPABASE_DATABASE_URL=${SUPABASE_DATABASE_URL}
SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
# Optional aliases for libraries that expect PUBLIC prefixed keys
SUPABASE_PUBLIC_ANON_KEY=${SUPABASE_ANON_KEY}
NEXT_PUBLIC_SUPABASE_URL=${SUPABASE_DATABASE_URL}
NEXT_PUBLIC_SUPABASE_DATABASE_URL=${SUPABASE_DATABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# Server-side secrets (never exposed to browsers)
SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
SUPABASE_JWT_SECRET=${SUPABASE_JWT_SECRET}
