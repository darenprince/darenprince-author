{
  "name": "darenprince-author",
  "version": "1.0.0",
  "description": "Author platform and website for Daren M. Prince.",
  "license": "CC-BY-NC-4.0",
  "scripts": {
    "build:search": "node ./src/search/build-index.mjs",
    "generate:images": "node scripts/generate-image-manifest.js",
    "generate:icons": "node scripts/generate-icons.mjs",
    "build": "npm run build:search && npm run generate:icons && npm run generate:images && sass --load-path=node_modules/@uswds/uswds/packages scss/styles.scss assets/styles.css",
    "watch": "npm run generate:icons && npm run generate:images && sass --load-path=node_modules/@uswds/uswds/packages scss/styles.scss assets/styles.css --watch",
    "test": "vitest run",
    "prepare": "command -v sass >/dev/null 2>&1 || npm install --no-save sass; husky install",
    "deploy": "netlify deploy --prod --site darenprince.netlify.app",
    "postprocess:assets": "node optimize-images.js --root .",
    "postprocess:seo": "node seo-enrich.js --root . --domain $DOMAIN",
    "format": "prettier --write .",
    "lint-staged": "lint-staged"
  },
  "devDependencies": {
    "@sendgrid/mail": "^7.7.0",
    "@types/node": "^24.2.0",
    "@uswds/uswds": "^3.13.0",
    "cheerio": "^1.0.0-rc.12",
    "favicons": "^7.1.6",
    "fs-extra": "^11.2.0",
    "glob": "^10.3.12",
    "globby": "^14.0.2",
    "gray-matter": "^4.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "marked": "^10.0.0",
    "micromatch": "^4.0.7",
    "minisearch": "^6.1.0",
    "netlify-cli": "^23.1.4",
    "prettier": "^3.3.3",
    "sass": "^1.89.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@uswds/uswds": "^3.13.0"
  },
  "type": "module",
  "lint-staged": {
    "*.{js,jsx,ts,tsx,css,scss,sass,json,md}": "prettier --write"
  }
}
