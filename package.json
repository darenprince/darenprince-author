{
  "scripts": {
    "build": "node scripts/generate-env.js && sass scss/styles.scss assets/styles.css",
    "watch": "node scripts/generate-env.js && sass scss/styles.scss assets/styles.css --watch",
    "test": "vitest run",
    "prepare": "command -v sass >/dev/null 2>&1 || npm install --no-save sass"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.53.0",
    "@types/node": "^24.2.0",
    "sass": "^1.89.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "@sendgrid/mail": "^7.7.0"
  }
}
